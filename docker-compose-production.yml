services:
  backend:
    env_file:
      - .env.production

  frontend:
    build:
      target: production
      args:
        NODE_ENV: production
        VITE_API_BASE_URL: ${VITE_API_BASE_URL}
    env_file:
      - .env.production