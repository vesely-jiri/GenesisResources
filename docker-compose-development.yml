services:
  backend:
    env_file:
      - .env.development

  frontend:
    build:
      target: development
      args:
        NODE_ENV: development
    env_file:
      - .env.development
    volumes:
      - ./frontend:/app
      - /app/node_modules